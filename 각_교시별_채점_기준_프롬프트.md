# 📝 각 교시별 평가 문항의 채점 기준 프롬프트

**최종 업데이트**: 2025년 1월  
**버전**: 1.1 (마법학교 컨셉 제거, 실제 사용 중인 교시명으로 업데이트)

---

## 📋 목차

1. [1교시: 알파벳 대소문자를 소리 내어 읽기 (p1_alphabet)](#1교시-알파벳-대소문자를-소리-내어-읽기-p1_alphabet)
2. [2교시: 단어를 듣고 올바른 단어 또는 알파벳 고르기 (p2_segmental_phoneme)](#2교시-단어를-듣고-올바른-단어-또는-알파벳-고르기-p2_segmental_phoneme)
3. [3교시: 단어를 듣고 올바른 강세 고르기 (p3_suprasegmental_phoneme)](#3교시-단어를-듣고-올바른-강세-고르기-p3_suprasegmental_phoneme)
4. [4교시: 무의미 단어, 단어, 문장을 소리 내어 읽기 (p4_phonics)](#4교시-무의미-단어-단어-문장을-소리-내어-읽기-p4_phonics)
5. [5교시: 단어, 어구, 문장을 듣거나 읽고 올바른 그림 고르기 (p5_vocabulary)](#5교시-단어-어구-문장을-듣거나-읽고-올바른-그림-고르기-p5_vocabulary)
6. [6교시: 대화를 듣거나 읽고, 질문에 대한 올바른 그림 고르기 (p6_comprehension)](#6교시-대화를-듣거나-읽고-질문에-대한-올바른-그림-고르기-p6_comprehension)

---

## 1교시: 알파벳 대소문자를 소리 내어 읽기 (p1_alphabet)

**평가 유형**: Letter Naming Fluency (LNF)  
**채점 방식**: 음성 인식 기반 자동 채점 (GPT-4o 사용)  
**파일 위치**: `src/app/api/submit-p1_alphabet/route.ts`

### 음성 전사 프롬프트 (OpenAI Whisper)

```
This is a DIBELS 8th edition Letter Naming Fluency (p1_alphabet) test for Korean EFL students. The student will name individual English letters.

CRITICAL INSTRUCTIONS:
1. Target letter: "${questionLetter}"
2. Accept Korean pronunciations: '에이' for A, '비' for B, '씨' for C, '디' for D, '이' for E, '에프' for F, '지' for G, '에이치' for H, '아이' for I, '제이' for J, '케이' for K, '엘' for L, '엠' for M, '엔' for N, '오' for O, '피' for P, '큐' for Q, '알' for R, '에스' for S, '티' for T, '유' for U, '브이' for V, '더블유' for W, '엑스' for X, '와이' for Y, '지' or '제트' for Z.
3. Accept English letter names: 'ay', 'bee', 'cee', 'dee', 'ee', 'eff', 'gee', 'aitch', 'eye', 'jay', 'kay', 'ell', 'em', 'en', 'oh', 'pee', 'cue', 'ar', 'ess', 'tee', 'you', 'vee', 'double-u', 'ex', 'why', 'zee'.
4. Be flexible with hesitations, repetitions, and partial attempts.
5. Return strict JSON with keys: "text" (string), "confidence" (string), and "segments" (array of {"start": number, "end": number, "text": string}) where start/end are seconds from audio start. Always include the segments array (empty if no speech).
```

### GPT-4o 채점 기준 프롬프트

```
You are a DIBELS 8th edition Letter Naming Fluency (p1_alphabet) scorer for Korean EFL students.

Scoring rules:
- ONLY letter NAMES are correct. Letter sounds must be categorised as "Letter sounds".
- Accept Korean pronunciations of letter names ONLY when they match the exact letter name (예: '에이' for A, '비' for B, '씨' for C, '디' for D, '이' for E, '에프' for F, '지' for G, '에이치' for H, '아이' for I, '제이' for J, '케이' for K, '엘' for L, '엠' for M, '엔' for N, '오' for O, '피' for P, '큐' for Q, '알' for R, '에스' for S, '티' for T, '유' for U, '브이' for V, '더블유' for W, '엑스' for X, '와이' for Y, '지' or '제트' for Z).
- CRITICAL: For letter 'E', accept '이' (i) as correct Korean pronunciation. This is the Korean way of pronouncing the letter name 'E'.
- CRITICAL: For letter 'D', only accept '디' (di) as correct Korean pronunciation. '드' (duh) is a letter sound and must be marked as incorrect. If the transcript shows 'D' but the pronunciation sounds like '드' (duh), mark it as incorrect.
- CRITICAL: For letter 'I', only accept '아이' (ai) as correct Korean pronunciation. '이' (i) is a letter sound and must be marked as incorrect. However, note that '이' is correct for letter 'E', not for 'I'.
- CRITICAL: For letter 'S', only accept '에스' (es) as correct Korean pronunciation. '스' (s) is a letter sound and must be marked as incorrect. If the transcript shows 'S' but the pronunciation sounds like '스' (s), mark it as incorrect.
- CRITICAL: If the transcript shows a letter name (e.g., 'J', 'Q', 'R') but the timeline shows no actual speech segments or very short duration (< 0.3 seconds), mark as "Omissions" (incorrect). This indicates the transcription might be hallucinated or from background noise.
- CRITICAL: If the transcript shows the correct letter name but the timeline segments are empty or show no meaningful speech (total speech duration < 0.3 seconds), mark as "Omissions" (incorrect). Do not trust transcripts that have no corresponding speech segments.
- CRITICAL: If the total speech duration across all timeline segments is less than 0.3 seconds, always mark as "Omissions" (incorrect) regardless of the transcript content, as this indicates silence or insufficient audio input.
- SPECIAL RULE for similar-sounding letters: For letters where the letter name and letter sound are phonetically similar (e.g., 'O': "oh" vs "ah", 'I': "eye/아이" vs "ih/이"), if the student's response is phonetically close to the letter name, accept it as correct. However, for 'I', '이' is NOT acceptable - only '아이' is correct.
- IMPORTANT: When the transcript shows a single letter (e.g., 'D', 'S'), you must analyze the actual pronunciation from the audio context. If it sounds like a letter sound (e.g., '드', '스') rather than a letter name (e.g., '디', '에스'), mark it as incorrect.
- Hesitation threshold is 5 seconds from audio start to first meaningful attempt.
- If a student self-corrects to the correct letter name within 5 seconds of their first incorrect attempt, mark the response correct and set "used_self_correction" to true.
- If the first meaningful attempt occurs after 5 seconds, the correct response is overridden by "Hesitation".
- Error categories must be one of: "Letter reversals", "Letter sounds", "Omissions", "Hesitation", "Other". Use "Other" only when no other category fits.

Return strict JSON: {
  "final_score": "correct" | "incorrect",
  "error_category": null | "Letter reversals" | "Letter sounds" | "Omissions" | "Hesitation" | "Other",
  "used_self_correction": boolean,
  "self_correction_within_seconds": number | null,
  "recognized_form": string | null,
  "notes": string | null
}
```

### 주요 채점 규칙 요약

1. **문자 이름 vs 문자 소리 구분**
   - 문자 이름만 정답으로 인정
   - 문자 소리는 "Letter sounds" 오류로 분류

2. **한국어 발음 인정 기준**
   - 각 알파벳의 정확한 한국어 이름 발음만 인정
   - 예: A → '에이', B → '비', E → '이', I → '아이'

3. **특수 규칙**
   - E: '이' 인정 (한국식 발음)
   - D: '디'만 인정, '드'는 문자 소리로 간주
   - I: '아이'만 인정, '이'는 문자 소리로 간주
   - S: '에스'만 인정, '스'는 문자 소리로 간주

4. **무음/불충분 음성 처리**
   - 총 음성 길이가 0.3초 미만이면 "Omissions"로 처리
   - 타임라인에 음성 세그먼트가 없으면 "Omissions"로 처리

5. **망설임(Hesitation) 처리**
   - 음성 시작 후 5초 이내에 의미 있는 시도가 없으면 "Hesitation"으로 처리
   - 5초 이내에 자기 수정(self-correction)이 있으면 정답으로 인정

6. **오류 유형 분류**
   - Letter reversals: 문자 뒤바꿈
   - Letter sounds: 문자 소리로 발음
   - Omissions: 무응답/불충분 음성
   - Hesitation: 망설임
   - Other: 기타

---

## 2교시: 단어를 듣고 올바른 단어 또는 알파벳 고르기 (p2_segmental_phoneme)

**평가 유형**: Phonemic Segmentation Fluency (PSF)  
**채점 방식**: 선택지 비교 (단순 문자열 일치)  
**파일 위치**: `src/app/api/submit-p2_segmental_phoneme/route.ts`

### 채점 기준

```typescript
const isCorrect = selectedAnswer === correctAnswer;
```

### 채점 규칙 요약

1. **단순 문자열 일치 비교**
   - 학생이 선택한 답안(`selectedAnswer`)과 정답(`correctAnswer`)을 문자열로 비교
   - 완전히 일치하면 정답, 아니면 오답

2. **오류 유형**
   - 넘어가기(Skipped): `skip === true`인 경우
   - 일반 오답: 선택지가 정답과 일치하지 않는 경우

3. **특별한 채점 로직 없음**
   - 음성 인식이나 복잡한 분석 없이 단순 비교만 수행
   - 프론트엔드에서 선택한 답안을 그대로 사용

---

## 3교시: 단어를 듣고 올바른 강세 고르기 (p3_suprasegmental_phoneme)

**평가 유형**: Stress Pattern Recognition (STRESS)  
**채점 방식**: 선택지 비교 (단순 문자열 일치)  
**파일 위치**: `src/app/api/submit-p3_suprasegmental_phoneme/route.ts`

### 채점 기준

```typescript
const isCorrect = selectedAnswer === correctAnswer;
```

### 채점 규칙 요약

1. **단순 문자열 일치 비교**
   - 학생이 선택한 강세 패턴(`selectedAnswer`)과 정답(`correctAnswer`)을 문자열로 비교
   - 완전히 일치하면 정답, 아니면 오답

2. **오류 유형**
   - 넘어가기(Skipped): `skip === true`인 경우
   - 일반 오답: 선택한 강세 패턴이 정답과 일치하지 않는 경우

3. **특별한 채점 로직 없음**
   - 음성 인식이나 복잡한 분석 없이 단순 비교만 수행
   - 프론트엔드에서 선택한 답안을 그대로 사용

---

## 4교시: 무의미 단어, 단어, 문장을 소리 내어 읽기 (p4_phonics)

**평가 유형**: Phonics (NWF, WRF, ORF 통합)  
**채점 방식**: 음성 인식 기반 간단한 채점  
**파일 위치**: `src/app/api/submit-p4_phonics/route.ts`

### 채점 기준

```typescript
// 음성 전사 프롬프트
const transcriptionPrompt = `This is a reading fluency test for Korean EFL students. The student will read ${testType === 'nwf' ? 'a nonsense word' : testType === 'wrf' ? 'a real word' : 'a sentence'}.

TARGET: "${question}"

Accept Korean-accented pronunciations and be flexible with variations.`;

// 간단한 정확도 평가
const isCorrect = !hesitationDetected && studentAnswer.toLowerCase().includes(question.toLowerCase().split(' ')[0]);
```

### 채점 규칙 요약

1. **음성 인식 기반 채점**
   - OpenAI Whisper API를 사용하여 음성을 텍스트로 전사
   - 한국식 발음과 변형을 허용하도록 프롬프트 설정

2. **정확도 판단 기준**
   - 망설임(Hesitation)이 없어야 함
   - 전사된 답안이 정답의 첫 번째 단어를 포함해야 함
   - 대소문자 구분 없이 비교

3. **망설임 감지**
   - 음성 시작 후 5초 이내에 의미 있는 시도가 없으면 망설임으로 간주
   - 망설임이 있으면 자동으로 오답 처리

4. **오류 유형**
   - Skipped: 넘어가기
   - Hesitation: 망설임 (5초 이내 응답 없음)
   - Other: 일반 오답 (전사 결과가 정답을 포함하지 않음)
   - insufficient_audio: 음성 데이터 불충분

5. **제한사항**
   - 음성 파일 크기: 1KB ~ 10MB 사이
   - 빈 음성 파일은 자동으로 Hesitation으로 처리

---

## 5교시: 단어, 어구, 문장을 듣거나 읽고 올바른 그림 고르기 (p5_vocabulary)

**평가 유형**: Vocabulary/Meaning (MEANING)  
**채점 방식**: 선택지 비교 (단순 문자열 일치)  
**파일 위치**: `src/app/api/submit-p5_vocabulary/route.ts`

### 채점 기준

```typescript
const isCorrect = selectedAnswer === correctAnswer;
```

### 채점 규칙 요약

1. **단순 문자열 일치 비교**
   - 학생이 선택한 그림 설명(`selectedAnswer`)과 정답(`correctAnswer`)을 문자열로 비교
   - 완전히 일치하면 정답, 아니면 오답

2. **오류 유형**
   - 넘어가기(Skipped): `skip === true`인 경우
   - 일반 오답: 선택한 그림이 정답과 일치하지 않는 경우

3. **특별한 채점 로직 없음**
   - 음성 인식이나 복잡한 분석 없이 단순 비교만 수행
   - 프론트엔드에서 선택한 답안을 그대로 사용

---

## 6교시: 대화를 듣거나 읽고, 질문에 대한 올바른 그림 고르기 (p6_comprehension)

**평가 유형**: Comprehension (COMPREHENSION)  
**채점 방식**: 선택지 비교 (단순 문자열 일치)  
**파일 위치**: `src/app/api/submit-p6_comprehension/route.ts`

### 채점 기준

```typescript
const isCorrect = selectedAnswer === correctAnswer;
```

### 채점 규칙 요약

1. **단순 문자열 일치 비교**
   - 학생이 선택한 답안(`selectedAnswer`)과 정답(`correctAnswer`)을 문자열로 비교
   - 완전히 일치하면 정답, 아니면 오답

2. **오류 유형**
   - 넘어가기(Skipped): `skip === true`인 경우
   - 일반 오답: 선택한 답안이 정답과 일치하지 않는 경우

3. **특별한 채점 로직 없음**
   - 음성 인식이나 복잡한 분석 없이 단순 비교만 수행
   - 프론트엔드에서 선택한 답안을 그대로 사용

4. **데이터 저장 형식**
   - `question` 필드에 대화/이야기와 질문을 함께 저장: `${dialogueOrStory} | ${question}`

---

## 📊 채점 방식 요약표

| 교시 | 평가 유형 | 채점 방식 | AI 사용 여부 | 주요 특징 |
|------|----------|----------|------------|----------|
| 1교시 | 알파벳 대소문자를 소리 내어 읽기 (LNF) | 음성 인식 + GPT-4o 채점 | ✅ | 문자 이름/소리 구분, 한국어 발음 인정, 망설임 감지 |
| 2교시 | 단어를 듣고 올바른 단어 또는 알파벳 고르기 (PSF) | 선택지 비교 | ❌ | 단순 문자열 일치 비교 |
| 3교시 | 단어를 듣고 올바른 강세 고르기 (STRESS) | 선택지 비교 | ❌ | 단순 문자열 일치 비교 |
| 4교시 | 무의미 단어, 단어, 문장을 소리 내어 읽기 (NWF/WRF/ORF) | 음성 인식 + 간단한 채점 | ✅ | 망설임 감지, 단어 포함 여부 확인 |
| 5교시 | 단어, 어구, 문장을 듣거나 읽고 올바른 그림 고르기 (MEANING) | 선택지 비교 | ❌ | 단순 문자열 일치 비교 |
| 6교시 | 대화를 듣거나 읽고, 질문에 대한 올바른 그림 고르기 (COMPREHENSION) | 선택지 비교 | ❌ | 단순 문자열 일치 비교 |

---

## 🔍 주요 채점 로직 상세

### 1교시 (p1_alphabet) - 가장 복잡한 채점

1. **음성 전사**: OpenAI Whisper API 사용
2. **GPT-4o 채점**: 전사 결과와 타임라인을 분석하여 채점
3. **후처리 검증**: 
   - 전사 결과가 정답 목록에 정확히 일치하는지 확인
   - 총 음성 길이가 0.3초 미만이면 오답 처리
   - GPT 판단이 잘못된 경우 자동 보정

### 4교시 (p4_phonics) - 간단한 음성 인식 채점

1. **음성 전사**: OpenAI Whisper API 사용
2. **간단한 포함 여부 확인**: 전사 결과가 정답의 첫 단어를 포함하는지 확인
3. **망설임 감지**: 5초 이내 응답 없으면 오답 처리

### 2, 3, 5, 6교시 - 단순 선택지 비교

- 프론트엔드에서 선택한 답안을 그대로 받아서 정답과 문자열 비교
- 별도의 AI 처리 없음

---

## 📝 참고 사항

1. **1교시와 4교시만 음성 인식 사용**
   - 1교시: GPT-4o를 사용한 정교한 채점
   - 4교시: 간단한 포함 여부 확인

2. **나머지 교시는 선택지 기반**
   - 프론트엔드에서 선택한 답안을 그대로 사용
   - 서버에서 단순 문자열 비교만 수행

3. **망설임(Hesitation) 처리**
   - 1교시와 4교시에서만 망설임 감지
   - 5초 이내 응답 없으면 자동 오답 처리

4. **오류 유형 분류**
   - 1교시: Letter reversals, Letter sounds, Omissions, Hesitation, Other
   - 4교시: Skipped, Hesitation, Other, insufficient_audio
   - 나머지: Skipped, null (일반 오답)

---

**문서 작성일**: 2025년 1월  
**작성자**: AIEEBSS 개발팀
